<% content_for(:page_title, t('service.title', page_title: 'Schedules')) %>
<div class="page-header">
  <ol class="breadcrumb">
    <li><a href="<%= root_path %>">Appointment planner</a></li>
    <li class="active">Schedules</li>
  </ol>

  <h1>Schedules</h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="centered-table table table-bordered table-striped">
      <colgroup>
        <col width="65%" />
        <col width="20%" />
        <col width="15%" />
      </colgroup>
      <thead>
        <tr class="table-header">
          <th>Location</th>
          <th>Schedule</th>
          <th><span class="sr-only">Actions</span></th>
        </tr>
      </thead>
      <tbody>
        <%= render 'location', location: @booking_location %>

        <% @child_locations.each do |location| %>
          <%= render 'location', location: location %>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
