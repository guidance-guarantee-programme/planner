<li class="activity-feed__item t-activity" id="activity-<%= appointment_mail_activity.id %>">
  <span class="glyphicon glyphicon-envelope" aria-hidden="true"></span>
  <strong>The system</strong> sent an email to the customer informing them
  their appointment has been <%= appointment_mail_activity.message %>
  <span class="activity-feed__date"><%= timestamp(appointment_mail_activity.created_at) %></span>
</li>
