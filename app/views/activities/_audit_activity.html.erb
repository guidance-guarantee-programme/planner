<li class="activity-feed__item t-activity" id="activity-<%= audit_activity.id %>">
  <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
  <b><%= audit_activity.owner_name %></b> <%= link_to "changed the #{audit_activity.message}", appointment_changes_path(audit_activity.booking_request), class: 't-changes' %>
  <span class="activity-feed__date"><%= timestamp(audit_activity.created_at) %></span>
</li>
