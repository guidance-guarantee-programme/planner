The appointment was changed by an agent

Reference number:
<%= @appointment.reference %>

The following details were changed:

<% @appointment.own_and_associated_audits.last.audited_changes.keys.map(&:humanize).each do |key| %>
* <%= key %>
<% end %>
